syntax = "proto3";

enum MsgID
{
    CS_HEART_BEAT           = 0; // 心跳
    CS_PLAYER_APPLY_TOKEN   = 1; // 申请TOKEN
    CS_PLAYER_AUTH          = 2; // 验证TOKEN
    CS_PLAYER_LOGIN_GAME    = 3; // 用户登陆账号

    CS_PLAYER_SEND_MESSAGE  = 103; // 用户发消息
    CS_PLAYER_REQ_MESSAGE   = 104; // 用户拉消息
    CS_PLAYER_REQ_CHAT_INFO = 105; // 用户查询当前状态

    SC_NOTIFY           = 10000; // 下推
}